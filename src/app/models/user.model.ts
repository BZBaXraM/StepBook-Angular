export interface User {
	Username: string;
	Token: string;
	token: string;
	accessToken: string;
	PhotoUrl?: string;
	KnownAs: string;
	Gender: string;
	RefreshToken: string;
	refreshToken: string;
	DateExpired: Date;
	dateExpired: Date;
}
