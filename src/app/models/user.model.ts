export interface User {
	Username: string;
	username: string;
	Token: string;
	token: string;
	accessToken: string;
	PhotoUrl?: string;
	photoUrl?: string;
	KnownAs: string;
	knownAs: string;
	Gender: string;
	gender: string;
	RefreshToken: string;
	refreshToken: string;
}
