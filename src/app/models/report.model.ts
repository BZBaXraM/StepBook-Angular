export interface Report {
	Reason: string;
}

export interface ReportDto {
	ReporterUsername: string;
	reporterUsername: string;
	ReportedUsername: string;
	reportedUsername: string;
	Reason: string;
	reason: string;
	CreatedAt: Date;
	createdAt: Date;
}
